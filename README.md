# Browser userscripts/styles

User-managed source files for browser extensions live here.

- `styles/`: Stylus `.user.css` files.
- `scripts/`: Violentmonkey `.user.js` files.

Keep these files as the source of truth; let the browser manage extension databases.
